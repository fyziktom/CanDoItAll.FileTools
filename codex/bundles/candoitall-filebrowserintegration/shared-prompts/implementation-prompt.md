# Implementation Prompt

Implement only the selected subbundle from `C:/programovani/candoitall-filebrowserintegration`.

Before editing, read the raw note rows it owns, the phase plan, all C# architecture files, its README, and prior critical proof. Run the entry gate and stop if a prerequisite or exact source anchor is stale. Keep `C:/repositories/CanDoItAll` read-only and preserve unrelated worktree changes.

Make the smallest cohesive change that satisfies the named responsibility. Contracts move before implementations; no new runtime partials, service location, cross-repository source references, concrete storage dependencies in FileTools, direct provider-effect anchors, or silent scope narrowing. Capture failing-first and passing behavior proof, hashes, source assertions, anti-stub audit, browser evidence where required, and a dependent smoke. Update the manifest, semantic invariants, execution report, raw-note rows, architecture checkpoint, and closure gate while evidence is fresh.

Stop and reopen the owning foundation when a cycle, contract leak, stale-cache behavior, host-action bypass, untestable extraction, or visual defect appears.

